# Default values for backend.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: localhost:32000/scraper-backend
  tag: latest
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  annotations: {}
  host: app.elasticdata.io

  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

pv:
  fileStorage:
    path: /opt/kubernetes/storage/fileStorage


resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

livenessProbe:
  initialDelaySeconds: 90
  periodSeconds: 10
readinessProbe:
  initialDelaySeconds: 60
  periodSeconds: 15

env:
  STATIC_RUN_DIRECTORY: /tmp/scraper-service

  GOOGLE_SIGNIN_CALLBACK_URL: http://app.elasticdata.io/#/enter/
  FACEBOOK_SIGNIN_CALLBACK_URL: http://app.elasticdata.io/#/enter/
  SELENIUM_DEFAULT_ADDRESS: http://selenoid.selenoid:4444/wd/hub

  RABBITMQ_QUEUE_PIPELINE_TASK_RUN: PIPELINE_TASK_RUN
  RABBITMQ_QUEUE_PIPELINE_TASK_STOP: PIPELINE_TASK_STOP
  RABBITMQ_QUEUE_PIPELINE_TASK_FINISHED: PIPELINE_TASK_FINISHED
  RABBITMQ_QUEUE_PIPELINE_RUN_HOOKS: PIPELINE_RUN_HOOKS
  RABBITMQ_QUEUE_PIPELINE_FINISHED: PIPELINE_FINISHED
  RABBITMQ_QUEUE_TASK_CHANGED: TASK_CHANGED

  RABBITMQ_ROUTING_PIPELINE_TASK_FINISHED: PIPELINE_TASK_FINISH.*
  RABBITMQ_ROUTING_PIPELINE_TASK_RUN: PIPELINE_TASK_RUN.*
  RABBITMQ_ROUTING_PIPELINE_TASK_STOP: PIPELINE_TASK_STOP.*
  RABBITMQ_ROUTING_PIPELINE_FINISHED: PIPELINE_FINISH.*
  RABBITMQ_ROUTING_TASK_CHANGED: TASK_CHANGED.*

  ES_HOST_NAME: elasticsearch-master.elasticsearch
  ES_HTTP_PORT: 9200
  ES_ENABLED: true

  PROXY_SERVICE_URL: "http://proxy"

  MINIO_URL: https://storage.elasticdata.io
  MINIO_PUBLIC_URL: https://storage.elasticdata.io
  MINIO_ACCESS_KEY: elasticdataio
  MINIO_SECRET_KEY: wJalrXUtnFEMI/9874c97c907209cpuvpw97bpb/asdasdasdasdasd
  APP_VERSION: "1.0.0"